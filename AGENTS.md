# SprayLOG — Agent Rules

## Release gate (user directive, 2026-08-07)

**Never trigger a release build/upload without the user's explicit go-ahead in the current conversation.** Present what would be released (version, contents) and wait for acceptance. Batch changes into as few releases as possible.
